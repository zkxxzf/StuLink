# StuLink — 隐私与安全审计报告

- **审计日期**: 2026-08-07
- **审计版本**: v1.7.0（2026-08-02）
- **审计范围**: 主应用 `app/`、往届查询 `alumni_app/`、配置、密钥管理、git 历史、部署包、文档附件

---

## 一、审计结论

**总体评级：✅ 通过（低风险）**

系统在密钥管理、密码存储、传输保护、访问控制、敏感数据加密、审计日志等方面已具备完整的安全基线；审计发现 1 项**已处理**的隐私风险（学生数据导出文件误置于 docs/）和 3 项**建议关注**的配置提示，均不影响发布。git 历史与代码仓库中**未发现任何**数据库文件、密钥或学生个人数据。

---

## 二、逐项审计结果

### 1. 密钥与凭证管理 — ✅ 通过

| 项目 | 结果 | 说明 |
|------|------|------|
| SECRET_KEY | ✅ | 环境变量优先 → `data/.secret_key` 文件 → 自动生成并持久化；无硬编码 |
| ENCRYPTION_KEY | ✅ | 环境变量优先 → `data/.encryption_key` 文件 → 自动生成；无硬编码 |
| 密钥文件入库 | ✅ | `data/`、`.secret_key`、`.encryption_key` 均在 `.gitignore` 中；git 历史无任何提交记录 |
| 默认管理员密码 | ⚠️ | `admin/admin123` 为首次部署初始凭证（`app/__init__.py`），文档已强制提示首次登录修改；`must_change_pwd` 字段已预留强制改密能力 |

### 2. 密码存储 — ✅ 通过

- 用户密码使用 Werkzeug `generate_password_hash`（PBKDF2-SHA256）存储，**无明文密码入库**

### 3. 传输与会话安全 — ✅ 通过（附提示）

| 项目 | 结果 | 说明 |
|------|------|------|
| Session Cookie | ✅ | `HttpOnly=True` + `SameSite=Lax` |
| CSRF 防护 | ✅ | 全局启用（Flask-WTF），所有表单与 JSON 接口校验 |
| 安全响应头 | ✅ | `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`X-XSS-Protection`、`Referrer-Policy` |
| Cookie Secure | ⚠️ 提示 | `SESSION_COOKIE_SECURE=False` 为内网 HTTP 环境所必需；若部署到公网 HTTPS，应改为 `True` |
| CSRF Token 有效期 | ⚠️ 提示 | `WTF_CSRF_TIME_LIMIT=None`（不过期）为兼容各浏览器会话保持；如需更高安全性可设置有效期 |

### 4. 认证与访问控制 — ✅ 通过

- 登录限流：每 IP 每分钟最多 10 次尝试（`app/modules/auth/routes.py`）
- 开放重定向防护：`_is_safe_redirect` 校验 `next` 参数仅允许同站相对路径
- 权限体系：`perm_required` 装饰器 + 权限组（管理员/年级长/班主任/宿管/任课教师），`User.has_perm` 统一校验
- 生产模式：默认 `waitress` 启动；`--dev` 调试模式显式开启并仅绑定 `127.0.0.1`

### 5. 敏感数据保护 — ✅ 通过

- 身份证号：AES-256-CBC **加密存储**（`app/utils/crypto.py`），确定性 IV 支持等值查询，Python 侧透明加解密
- 导出控制：身份证/电话/毕业学校等敏感列需独立权限（`students.export_id_card` / `export_phone` / `export_graduation_school` / `export_enrollment`）
- 数据范围隔离：班主任仅能查询/导出本班学生（`export_helpers.py` 按角色过滤）
- 上传限制：`MAX_CONTENT_LENGTH = 16MB`

### 6. 审计日志 — ✅ 通过

- 登录/登出、密码修改、学生增删改、分配操作、导出操作均记录操作日志（含操作人、IP、详情）
- 导出日志记录所选列与过滤条件，可追溯敏感数据外发

### 7. 依赖与运行时 — ✅ 通过

- 生产使用 Waitress（纯 Python WSGI，无已知公开高危配置问题）
- 数据库访问池化（pool_size=10、pre_ping）
- Docker 镜像以 `python:3.11-slim` 为基础，仅复制应用代码，**不含** `data/` 与密钥（`.dockerignore` 已排除）

### 8. git 仓库与文档附件 — ✅ 通过（1 项已处理）

| 项目 | 结果 | 说明 |
|------|------|------|
| 数据库入库 | ✅ | `git log --all` 无 `data/*.db`、密钥文件历史 |
| 学生数据附件 | ✅ **已处理** | 发现 `docs/宿舍床铺分配明细_20260731_134601.xlsx`（931 行真实学生学号+姓名+床位），已移入 git 忽略的 `data/backups/`，并新增 `.gitignore` 规则 `docs/*.xlsx` 防止再犯 |
| 临时文件 | ✅ 已清理 | 18 个 `_tmp_*` 开发调试文件已删除，并新增 `_tmp_*` / `test_debug.py` 忽略规则 |
| IDE 目录 | ✅ | `.trae/` 已加入 `.gitignore` |
| 截图附件 | ✅ | `docs/*.png` 已被 `*.png` 规则忽略，不会上传 |

---

## 三、遗留建议（非阻断项）

1. **部署到公网 HTTPS 后**：将 `SESSION_COOKIE_SECURE` 改为 `True`，并考虑设置 `WTF_CSRF_TIME_LIMIT` 有效期
2. **首次部署**：登录后立即修改 `admin` 默认密码；正式环境建议通过环境变量注入 `SECRET_KEY` / `ENCRYPTION_KEY`（`docker-compose.yml` 已预留）
3. **备份策略**：`data/backups/` 含毕业备份与学生数据导出，请纳入离线备份并限制访问权限
4. **依赖更新**：建议定期执行 `pip list --outdated`，关注 Flask / Werkzeug / cryptography 安全公告

---

## 四、审计方法

- 静态代码审查：配置、认证、加密、导出、权限模块逐文件检查
- 敏感模式扫描：硬编码密码/密钥/Token 的正则扫描（未发现）
- git 历史审计：`git log --all` 全量文件名检查（未发现 `.db`/密钥/`.xlsx`/学生数据）
- 附件检查：docs 目录二进制附件逐项人工核对

*本报告由 AI 辅助生成，结论基于 v1.7.0 代码快照；版本升级后应重新审计。*
