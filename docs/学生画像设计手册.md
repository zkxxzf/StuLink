# 学生画像设计手册

> 版本：v1.18.2.0（2026-09-24）｜状态：**规划中（占位）**（占位文档，待模块开发时编写全文）
> 所属模块：`portrait`（独立库 `portrait.db`）｜页面前缀：`/portrait`
> 文档体系：`设计文档.md`（总手册）→ 本手册（模块手册）

## 1. 模块定位与边界

**规划做什么（方向）：**
- 学习画像：成绩趋势、学科均衡度、进退步分析（数据来源 `grades.db`，按学号只读汇聚）
- 行为画像：积分表现、宿舍表现、出勤记录等综合表现（来源 `points.db` / `dormitory.db`）
- 成长档案：荣誉、教师评语、关键事件记录（本模块自有数据）

**边界原则：** 画像数据**按学号只读汇聚**各模块数据，不改写来源模块；任一来源模块异常时画像降级展示（缺该维度）而非崩溃。

## 2. 数据库设计（portrait.db，占位）

| 表 | 说明 |
|----|------|
| `student_profiles` | 占位表：学号（唯一）、画像摘要（JSON 预留）、时间戳 |

〔待模块开发时设计：画像维度数据表、评语表、事件表等〕

## 3. 权限与数据范围

〔待定〕预期：
- 学生画像查看：建议限班主任（本班）/年级长（本年级）/校级，且接入用户级数据范围（UserDataScope）
- 画像生成：教务/系统侧批量任务

## 4. 核心流程与页面

占位页 `/portrait/`：展示规划方向（学习/行为/成长三卡片）+ 说明。首页卡片状态"规划中"。
〔待开发：学生画像详情页、维度雷达图、趋势时间线〕

## 5. 与其他模块的数据交互

| 来源模块 | 数据 | 方式 |
|---------|------|------|
| 成绩管理 | 历次考试分数/排名/趋势 | 按学号只读（grades.db） |
| 德育管理 | 积分记录/汇总 | 按学号只读（points.db） |
| 宿舍管理 | 住宿与分配表现 | 按学号只读（dormitory.db） |
| 系统管理 | 学生基础信息 | 按学号只读（system.db） |

## 6. 故障隔离自检

- 独立库 `portrait.db`，来源模块只读汇聚，写入仅本库
- 任一来源模块异常 → 对应维度显示"暂无数据"，其余维度正常
- 本模块损坏不影响任何其他模块（含系统管理）

## 7. 版本演进记录

| 版本 | 内容 |
|------|------|
| v1.9.2 | 模块占位建立：独立库 portrait.db + 占位页面 + 首页入口（规划中） |
